#include "holberton.h"
#include <stdio.h>

/**
 * _strlen - returns the length of the string
 *@s: characters in string
 * Return: returns length of string
 **/

int _strlen(char *s)
{
	int i;

	for (i = 0; s[1] != 0; i++)
	{
	}
	return (i);
}
