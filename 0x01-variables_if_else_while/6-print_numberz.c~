#include <stdio.h>

/**
 * main - only function, contains commands
 *
 * Return: returns 0 to exit
 */

int main(void)
{
	int m;

	for (m = 48; m < 58; m++)
	{
		putchar(m);
	}
	putchar(10);
	return (0);
}
