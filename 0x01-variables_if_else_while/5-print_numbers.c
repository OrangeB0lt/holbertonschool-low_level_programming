#include <stdio.h>

/**
 * main - only function, contains commands
 *
 * Return: returns 0 to exit
 */

int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
		putchar (i);
	putchar('\n');
	return (0);
}
