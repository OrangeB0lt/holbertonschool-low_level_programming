#include <stdio.h>

/**
 * main - only function, contains commands
 *
 * Return: returns 0 to exit
 */

int main(void)
{
	int i;

	for (i = 122; i > 96; i--)
	{
		putchar(i);
	}
	putchar(10);
	return (0);
}
